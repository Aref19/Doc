

build:
  version: "30.0"
accounts:
  message: "prodServerChangedsaaref"

management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"


server:
  port: 8983
  
